<h1>Listing status_users</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Content</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @status_users.each do |status_user| %>
  <tr>
    <td><%= status_user.name %></td>
    <td><%= status_user.content %></td>
    <td><%= link_to 'Show', status_user %></td>
    <td><%= link_to 'Edit', edit_status_user_path(status_user) %></td>
    <td><%= link_to 'Destroy', status_user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Status user', new_status_user_path %>
